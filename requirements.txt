
# build dependencies (must come first)
setuptools>=69.0.0
wheel>=0.42.0
pip>=25.0.0

# pinned runtime deps (exact versions chosen for compatibility)
numpy>=1.26.4
pandas>=2.2.0
scikit-learn>=1.4.0
nltk>=3.8.1
matplotlib>=3.8.0
seaborn>=0.13.0
joblib>=1.3.0
streamlit>=1.29.0

# Temporarily remove wordcloud due to Python 3.13 compatibility issues

# test & linting (optional to install via requirements-dev.txt)
pytest==7.4.0
flake8==6.1.0
black==23.12.0
isort==5.12.0
